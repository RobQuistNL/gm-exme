///Check wether a control is active (e.g. being pushed)
control = getMappedControl(argument0); // Load up the mapped 

//for now we'll only use the keyboards' actions.
return keyboard_check(control);

